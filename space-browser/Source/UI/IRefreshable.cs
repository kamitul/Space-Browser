namespace space_browser.Source
{
    /// <summary>
    /// Refreshable interface for popups with refreshable functionalites
    /// </summary>
    interface IRefreshable
    {
        void Refresh();
    }
}
